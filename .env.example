# Database Configuration
DATABASE_URL=postgresql://jelantahgo:jelantahgo123@localhost/jelantahgo_db

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# JWT Secret (untuk authentication)
# IMPORTANT: Ganti dengan secret key yang kuat di production!
JWT_SECRET=your-secret-key-change-in-production

# Email Configuration (untuk notifications)
# Jika tidak dikonfigurasi, sistem akan tetap berjalan tetapi tidak mengirim email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com
